package patches.buildTypes

import jetbrains.buildServer.configs.kotlin.*
import jetbrains.buildServer.configs.kotlin.ui.*

/*
This patch script was generated by TeamCity on settings change in UI.
To apply the patch, change the buildType with id = 'FcalenderFrontendBuild'
accordingly, and delete the patch script.
*/
changeBuildType(RelativeId("FcalenderFrontendBuild")) {
    check(artifactRules == "frontend/android/app/build/outputs/apk/release/app-release.apk ") {
        "Unexpected option value: artifactRules = $artifactRules"
    }
    artifactRules = "frontend/android/app/build/outputs/apk/release/app-release.apk"
}
